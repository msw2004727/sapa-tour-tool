{
  "rules": {
    "trip": {
      ".read": true,
      ".write": true,
      "$doc": {
        ".validate": "newData.hasChildren() && newData.child('_ts').isNumber()"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
