{
  "rules": {
    "trip": {
      ".read": "auth != null",
      "$doc": {
        ".write": "auth != null && newData.exists()",
        ".validate": "newData.hasChildren() && newData.child('_ts').isNumber()"
      },
      "members": {
        ".write": "auth != null && newData.exists()",
        ".validate": "newData.hasChildren() && newData.child('_ts').isNumber() && newData.child('items').child('0').exists()"
      },
      "itinerary": {
        ".write": "auth != null && newData.exists()",
        ".validate": "newData.hasChildren() && newData.child('_ts').isNumber() && newData.child('items').child('0').exists()"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}